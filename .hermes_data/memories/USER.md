RULE: On every system verification run, append timestamped entry to Obsidian vault notes (## Timeline) with result, check, command, snippet. Vault: OBSIDIAN_VAULT_PATH or .hermes_data/obsidian-vault, file YYYY-MM-DD.md.
§
User on Replit: ephemeral dirs symlinked (.cache, .pythonlibs, .local, .config) to ~/, .pythonlibs included in symlink loop. After file reorg, proactively suggest adding new dirs (e.g. credentials/) to .gitignore.
§
User wants unredacted output — no masking/API key redaction. Show full keys, tokens, URLs in results.
§
Prefer bash scripts, minimal lines. "Just give me bash" — terse only. headless=False always, no xvfb (display running). If script corrupted by bad sed/patch, reset to bare version instead of iterating on broken code.
§
Browser automation: CloakBrowser (2026-06-29). `from cloakbrowser import launch, launch_persistent_context`. headless=False, humanize=True. Pattern: bash script generates .py helpers in ~ (like email.sh→~/duckmail.py), then runs them.
§
User stores credentials in ~/config.py (generated by email.sh) and workspace/credentials/. Uses Proton Mail for verification, Duck Address for temp emails.