# 2026-06-25 Session Notes

## Setup & Tools
- Obsidian vault initialized at .hermes_data/obsidian-vault
- Vault path persisted in .hermes_data/.env as OBSIDIAN_VAULT_PATH
- Vault is empty — first session using it as memory source

## What Happened
- User wants Obsidian vault as persistent notes layer: updated constantly, consulted for memory
- Memory nearly full (2111/2200 chars); freed room by removing stale Firecrawl item
- Resolved vault path to .hermes_data/obsidian-vault (no prior vault existed)
- Created directory + added env var via shell append
- Configured GitHub push auth via .pat token file stored as git credential store (--file=/tmp/git-creds.txt)
- Successfully pushed to origin/main after Replit askpass workaround

## Working With OWL
- User prefers direct edits via tools, concise responses, bash over python
- Session notes should be written here as we go, not batched at end

## Open Items
- None yet — first real session using the vault

## Commit & Push Log
- `051f998` Add obsidian vault, session notes, updated memory and environment config
- `d552d58` Document .pat-based GitHub auth workaround in session notes
- `b44e63a` Sync .hermes_data updates: logs, state, skills, environment-setups reference
- Push flow: `echo "https://oauth2:$(cat .pat)@github.com" > /tmp/git-creds.txt && git config credential.helper "store --file=/tmp/git-creds.txt" && git push origin main`
- /tmp may be wiped between sessions; recreate credential store before push if needed
