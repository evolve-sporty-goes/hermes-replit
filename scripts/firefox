URL=$(curl -sI -L 'https://download.mozilla.org/?product=firefox-latest-ssl&os=linux64&lang=en-US' | grep -i '^location:' | sed 's/location: //i' | tr -d '\r') && curl -L -o /tmp/firefox.tar.xz "$URL" && tar -xf /tmp/firefox.tar.xz -C /tmp && /tmp/firefox/firefox &